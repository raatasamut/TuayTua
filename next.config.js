/** @type {import('next').NextConfig} */
const nextConfig = {
  // The i18n configuration is now handled by the middleware.ts file
  // and the app/[locale] directory structure
};

module.exports = nextConfig;
